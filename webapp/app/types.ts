export type Faculty = { id: string; display_name: string; aliases: string[]; first_report_year: number | null; last_report_year: number | null };
export type Topic = { id: string; model_topic_id: number; label: string; description: string; publication_count: number; top_terms: string[]; representative_titles: string[] };
export type FacultyTopicEdge = { id: string; source: string; target: string; publication_count: number; share_of_faculty_clustered_publications: number; mean_topic_probability: number; recency_weighted_publication_count: number; first_publication_year: number | null; last_publication_year: number | null; publication_ids: string[] };
export type SimilarityEdge = { id: string; source: string; target: string; combined_similarity: number; embedding_similarity: number; topic_profile_similarity: number; shared_topic_ids: string[]; explanation: string };
export type Publication = { id: string; title: string; publication_year: number | null; doi: string | null; landing_url: string | null; abstract: string | null; authors: string[]; faculty_ids: string[]; topic_id: string; model_topic_id: number; topic_probability: number; coordinates: { x: number; y: number }; quality: { has_abstract: boolean; has_pdf: boolean; match_score: number | null } };
export type FacultyProfile = { faculty_id: string; display_name: string; publication_count: number; topic_count: number; topics: { target: string; publication_count: number; share_of_faculty_clustered_publications: number; mean_topic_probability: number; recency_weighted_publication_count: number }[] };
export type AtlasData = { faculty: Faculty[]; topics: Topic[]; facultyTopicEdges: FacultyTopicEdge[]; similarityEdges: SimilarityEdge[]; publications: Publication[]; profiles: FacultyProfile[] };
export type Selection = { type: "faculty" | "topic" | "publication"; id: string } | null;
